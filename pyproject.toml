[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "catalytic_world"
version = "0.1.0"
description = "TFM Catalytic World Project"
dependencies = [] 

[tool.setuptools]
py-modules = ["fun_gilles"]